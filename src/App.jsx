import { HomePage } from './pages/HomePage.jsx';

/**
 * Root application component.
 * Providers (Mantine, react-query) are set up in `main.jsx`, so this stays
 * focused on routing/page composition.
 */
const App = () => <HomePage />;

export default App;
